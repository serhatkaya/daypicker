{
  "name": "root",
  "description": "This is mono repo project for DayPicker Component, this package is not published to npm",
  "private": false,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "dev": "lerna run --scope='@serhatkaya/daypicker-core' start --stream",
    "server": "lerna run start-ts --stream",
    "build:all": "node scripts/build.js"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "lerna": "^8.1.2",
    "typescript": "^5.4.5"
  }
}
